# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'own-exhibition' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for own-exhibition
	pod 'RxSwift', '6.5.0'
	pod 'RxCocoa', '6.5.0'

  target 'own-exhibitionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'own-exhibitionUITests' do
    # Pods for testing
  end

end
